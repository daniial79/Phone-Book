version: '3.9'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.dev
      target: dev
    env_file:
      - .env
    ports:
      - '8000:8000'
    volumes:
      - .:/application

